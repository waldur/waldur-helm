apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "waldur.fullname" . }}-test-connection"
  labels:
{{ include "waldur.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "waldur.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
