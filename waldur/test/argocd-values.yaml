# Layered after test/values.yaml for the ArgoCD MR-preview environment.
# Installs the bundled PostgreSQL subchart instead of relying on the external
# database, without persistence so previews are disposable.
postgresql:
  enabled: true
  persistence:
    enabled: false
